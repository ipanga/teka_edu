# Active Task

<!--
  The checkpoint for the task in progress. A fresh Claude Code session reads this file, compares
  it with the repository, and resumes — see docs/RESUMABLE_WORKFLOW.md. The per-asset tracker of
  this task is docs/work/SEPTEMBER_VISUAL_QA.md (generated by `npm run visual:audit`).
-->

## Task

Final September visual QA before pedagogical reconfirmation — 1ère and 3ème maternelle.

## Objective

Every one of the 49 September pictures has an explicit final decision, weak ones are fixed, the
set is frozen (`SEPTEMBER_VISUAL_ASSETS_FROZEN_FOR_RECONFIRMATION`), and the reconfirmation
packages are regenerated from canonical content — once. Approvals are **not** restored here.

## Status

`awaiting_review`

## Branch

`feat/september-visual-ux` (PR #79, presentation infrastructure) and
`feat/september-illustrations` (PR #80, media assets, based on #79). Infrastructure changes land
on #79 first and are merged into #80 with a normal merge commit — no force push.

## Base Branch

`develop` at `087fc06`

## Started

2026-09-23

## Last Checkpoint

2026-09-23 — **Frozen** (`SEPTEMBER_VISUAL_ASSETS_FROZEN_FOR_RECONFIRMATION`). 49/49 decided,
packages regenerated, full verification green. Awaiting the owner's reconfirmation review.

## Scope

- Per-asset tracker (49 assets), regenerated and freshness-tested.
- Visual re-audit at phone, tablet, desktop and TV sizes; A accept / B refine / C redraw.
- Illustration style guide (`docs/ILLUSTRATION_STYLE_GUIDE.md`).
- Motion audit; fix content hidden during staggered entrances; E2E for reduced motion.
- Audio plan in four classes and a recording package generated from content.
- Walkthrough of four representative sessions; typography decision.
- Reconfirmation packages with per-lesson before/after hashes and a change summary.

## Out of Scope

- Restoring the 80 lapsed approvals; any pedagogical wording, objective or progression change;
  October; 2ème maternelle; production; synthetic or paid audio; merging PRs.

## Product Decisions

- The shapes stay byte-identical (a shape is a shape; re-rendering lapses nothing useful).
- System UI stays the typeface (decision recorded in the style guide).

## Completed

- [x] Resume checks and archive.
- [x] PR A: never-invisible stagger + E2E, TV child view, audio in four classes, recording
      script (`audio:brief`), child-view fixes from the walkthrough.
- [x] PR B: 14 pictures refined or redrawn, 0 new lapses; tracker; style guide; freeze;
      packages; migration `20260923175037_september_visual_upgrade`.
- [x] Full verification (below).

## In Progress

None.

## Remaining

- [ ] Owner: submit the two reconfirmation packages and the sheet (OI-001).
- [ ] After `accepted`: history entries, `scripts/approve-week.ts` per week, new data migration.
- [ ] Owner: merge #79 then #80 (retarget #80 to `develop` after #79 merges); staging then
      deploys and should be checked.

## Validation State

| Check              | Result | At                                          |
| ------------------ | ------ | ------------------------------------------- |
| format             | PASS   | working tree after freeze                   |
| lint               | PASS   | working tree after freeze                   |
| typecheck          | PASS   | working tree after freeze                   |
| unit tests         | PASS   | working tree after freeze — 385             |
| content validation | PASS   | working tree after freeze — 31 files        |
| database tests     | PASS   | local `db reset` — 152 pgTAP                |
| build              | PASS   | working tree after freeze (+ client bundle) |
| E2E                | PASS   | working tree after freeze — 31 (9 skipped)  |
| Docker             | PASS   | portable + Vercel images, both smoke-tested |
| secret scans       | PASS   | gitleaks 169 commits; 0 tracked `.env*`     |

## Database State

- Local: 42 migrations incl. `20260923175037_september_visual_upgrade` (regenerated; the
  earlier `20260922220249` was never applied to a hosted project and was replaced).
- DEV / PROD: 41. The migration reaches DEV when #80 merges into `develop`.

## Deployment State

- Production `a729722`, untouched. Staging on `develop` `087fc06`, untouched.

## Git State

- Both branches pushed, clean.

## Blockers

None.

## User Decisions Needed

None yet.

## Exact Resume Point

Nothing is running. The next action is the owner's: send
`docs/review/2026-2027-maternelle-1-reconfirmation-visuelle.md`,
`docs/review/2026-2027-maternelle-3-reconfirmation-visuelle.md` and
`docs/review/media/septembre-avant-apres.png` to the reviewer. If a picture must change: set
`finalQa.frozen` to false, redraw, re-freeze, `npm run review:visual`, submit again.

## Resume Verification

1. `git status --short`; `git branch --show-current`; `git log -n 5 --oneline`;
2. `npm run visual:audit` produces no diff;
3. `docs/work/SEPTEMBER_VISUAL_QA.md` — its resume point.
