# Active Task

<!--
  The checkpoint for the task in progress. A fresh Claude Code session reads this file, compares
  it with the repository, and resumes — see docs/RESUMABLE_WORKFLOW.md. The per-asset tracker of
  this task is docs/work/SEPTEMBER_VISUAL_QA.md (generated by `npm run visual:audit`).
-->

## Task

Close the September visual upgrade: restore the 3ème approvals, merge #79 and #80, verify staging.

## Objective

Every one of the 49 September pictures has an explicit final decision, weak ones are fixed, the
set is frozen (`SEPTEMBER_VISUAL_ASSETS_FROZEN_FOR_RECONFIRMATION`), and the reconfirmation
packages are regenerated from canonical content — once. Approvals are **not** restored here.

## Status

`completed`

## Branch

`feat/september-visual-ux` (PR #79, presentation infrastructure) and
`feat/september-illustrations` (PR #80, media assets, based on #79). Infrastructure changes land
on #79 first and are merged into #80 with a normal merge commit — no force push.

## Base Branch

`develop` at `087fc06`

## Started

2026-09-23

## Last Checkpoint

2026-09-23 — **Completed.** September 176/176 approved in canonical content, in a fresh local
database and on Supabase DEV. PRs #79 (`fdb4661`), #81 (`f7edc6d`) and #80 (`e882c10`)
squash-merged into `develop`; staging deployed green from `e882c10`. Production untouched.

## Scope

- Per-asset tracker (49 assets), regenerated and freshness-tested.
- Visual re-audit at phone, tablet, desktop and TV sizes; A accept / B refine / C redraw.
- Illustration style guide (`docs/ILLUSTRATION_STYLE_GUIDE.md`).
- Motion audit; fix content hidden during staggered entrances; E2E for reduced motion.
- Audio plan in four classes and a recording package generated from content.
- Walkthrough of four representative sessions; typography decision.
- Reconfirmation packages with per-lesson before/after hashes and a change summary.

## Out of Scope

- Restoring the 80 lapsed approvals; any pedagogical wording, objective or progression change;
  October; 2ème maternelle; production; synthetic or paid audio; merging PRs.

## Product Decisions

- The shapes stay byte-identical (a shape is a shape; re-rendering lapses nothing useful).
- System UI stays the typeface (decision recorded in the style guide).

## Completed

- [x] Resume checks and archive.
- [x] PR A: never-invisible stagger + E2E, TV child view, audio in four classes, recording
      script (`audio:brief`), child-view fixes from the walkthrough.
- [x] PR B: 14 pictures refined or redrawn, 0 new lapses; tracker; style guide; freeze;
      packages; migration `20260923184512_september_visual_upgrade`.
- [x] Full verification (below).
- [x] Reconfirmation result: 1ère 35 approvals restored (`--lapsed-only`); 3ème `m3-art-04-a1`
      corrected (`histoire-pluie` + `domain/lessons/pictures.ts`); media-consistency audit and test;
      3ème package regenerated; migration `20260923184512_september_visual_upgrade`.

## In Progress

None.

## Remaining

- [ ] Owner: prune the container registry (45 of 50 after this PR) before the next merge into
      `develop` — the next staging deploy stops at the 45 line (ISSUE-011).
- [ ] Follow-up: pin the Supabase CLI in `deploy-staging.yml` and `deploy-production.yml` too.

## Validation State

| Check              | Result | At                                                |
| ------------------ | ------ | ------------------------------------------------- |
| format             | PASS   | CI on `e882c10` and locally                       |
| lint               | PASS   | CI on `e882c10` and locally                       |
| typecheck          | PASS   | CI on `e882c10` and locally                       |
| unit tests         | PASS   | CI on `e882c10`; locally 395 in 30 files          |
| content validation | PASS   | CI on `e882c10`; 31 files, 0 digest mismatches    |
| database tests     | PASS   | CI on `e882c10` (fresh DB); locally 152 pgTAP     |
| build              | PASS   | CI on `e882c10`; client bundle check PASS         |
| E2E                | PASS   | local 31; staging live smoke 31 (9 skipped)       |
| Docker             | PASS   | CI on `e882c10`; locally both images smoke-tested |
| secret scans       | PASS   | gitleaks 173 commits, no leaks; 0 tracked `.env*` |

## Database State

- Local: 42 migrations; fresh `db reset`: 176 approved, 0 review, 152 pgTAP PASS.
- **DEV: 42 applied, identical to local** (incl. `20260923192024_september_visual_upgrade`);
  read-only data check: 176 lessons, 88 + 88 approved, 0 review, 176 distinct digests, all
  `ai-assisted`. (pgTAP is not installed on DEV and was not installed: no hosted schema edits.)
- PROD: untouched.

## Deployment State

- Staging: `dpl_14jzPbdFJSjZ7c7Ubb3YqpXrempd`, READY, alias teka-edu-staging.vercel.app, health
  asserted `staging`, the DEV ref and SHA `e882c10`; smoke E2E 31 passed.
- Production: LIVE, `a729722`, untouched.
- Container registry: 44 of 50 before this PR, 45 after it.

## Git State

- `develop` at `e882c10` (+ this docs PR). `main` at `a729722`. Feature branches merged.

## Blockers

None.

## User Decisions Needed

- Approve pruning the container registry before the next merge into `develop` (ISSUE-011).
- The next product task (see Exact Resume Point); a production release is a separate decision.

## Exact Resume Point

The September visual upgrade is closed. Next product task: **October for 3ème maternelle** —
refine October's slice of the annual plan (ISSUE-022), then author weeks 1–2 as a pilot and
send them through the review gate (`docs/ANNUAL_PLAN.md`, Plan §28). Before merging anything,
prune the container registry.

## Resume Verification

`gh pr checks 80`. When all four checks are green: `gh pr ready 80 && gh pr merge 80 --squash`,
then `gh run list --branch develop --workflow deploy-staging.yml --limit 1` and watch it.

## Resume Verification

1. `git status --short`; `git branch --show-current`; `git log -n 5 --oneline`;
2. `npm run visual:audit` produces no diff;
3. `docs/work/SEPTEMBER_VISUAL_QA.md` — its resume point.
